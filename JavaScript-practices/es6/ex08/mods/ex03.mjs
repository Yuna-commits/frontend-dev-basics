/**
 * Named Export
 * - 1. cjs(common js)의 exports, require()와 유사
 * - 2. import 하는 모듈에서 destruction 이 가능하다
 * - 3. export 시 이름을 저장한다
 * - 4. 다중 export가 가능하다
 */

export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
